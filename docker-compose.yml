
services:
  # Nodejs
  cv:
    build: .
    restart: always
    volumes:
      - "./src:/app/src"
    ports:
      -"8000:8000"

  # MongoDB
  mongo_db:
    container_name: db_container
    image: mongo:latest
    restart: always
    volumes:
      - "mongo_db:/data/db"